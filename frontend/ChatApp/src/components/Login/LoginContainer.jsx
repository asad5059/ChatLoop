import React from 'react';
import Login from './Login';

// TODO: Import Redux hooks when integrated
// import { useSelector, useDispatch } from 'react-redux';
// TODO: Import API helpers when backend is ready

const LoginContainer = () => {
  // Placeholder for API logic (e.g., login, fetch data)
  // const handleLogin = async (username) => { ... }

  // Placeholder for Redux state (future)
  // const user = useSelector(state => state.user);

  // Pass props and handlers to Login (presentational)
  return <Login /* onLogin={handleLogin} user={user} */ />;
};

export default LoginContainer; 